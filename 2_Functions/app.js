// Function Declaration.
function greet(name) {
  console.log("Привет, " + name + "!");
};

// Function Expression.
const greet = function(name) {
  console.log("Привет, " + name + "!");
};

// Стрелочная функция
// Пример стрелочной функции
const sum = (a, b) => {
  return a + b;
};